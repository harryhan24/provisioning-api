// @flow
import { Allocation, Project } from "../database/models";
import logger from "../utils/logger";

export default class AllocationService {
  static async createAllocation(project: Project) {
    return project;
  }
}
